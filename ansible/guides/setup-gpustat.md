gpustat is excellent for servers with multiple GPUs.

    Installation:
    Bash

    pip install gpustat

    Usage:
    Bash

    gpustat -cp --watch

        -c: Shows the process name.

        -p: Shows the PID.

        --watch: Updates the view continuously.
